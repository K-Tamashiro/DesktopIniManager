<Window x:Class="DesktopIniManager.Views.GrepWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:history="clr-namespace:DesktopIniManager.Views" xmlns:res="clr-namespace:DesktopIniManager.Properties" Title="{x:Static res:Strings.Grep_Title}" Icon="../DesktopIniManager.ico" Width="1280" Height="800" MinWidth="850" MinHeight="560" Background="{DynamicResource WindowBackground}" Foreground="{DynamicResource Ink}" WindowStartupLocation="CenterOwner">
  <Window.Resources>
    <Style TargetType="{x:Type ComboBoxItem}">
      <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
      <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type ComboBoxItem}">
        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border>
      </ControlTemplate></Setter.Value></Setter>
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource ThemeSelected}"/></Trigger>
        <Trigger Property="IsHighlighted" Value="True"><Setter Property="Background" Value="{DynamicResource AccentSoft}"/></Trigger>
        <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
      </Style.Triggers>
    </Style>
    <Style TargetType="{x:Type ComboBox}">
      <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
      <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
      <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type ComboBox}">
        <Grid>
          <ToggleButton x:Name="DropDownToggle" Focusable="False" ClickMode="Press"
                        IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
            <ToggleButton.Template><ControlTemplate TargetType="{x:Type ToggleButton}">
              <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                <Path Data="M 0 0 L 4 4 L 8 0" Stroke="{DynamicResource Ink}" StrokeThickness="1.5" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
              </Border>
            </ControlTemplate></ToggleButton.Template>
          </ToggleButton>
          <Border Padding="{TemplateBinding Padding}" Margin="0,0,28,0" IsHitTestVisible="False">
            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
          </Border>
          <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False"
                 IsOpen="{TemplateBinding IsDropDownOpen}" PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
            <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource Line}" BorderThickness="1"
                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
              <ScrollViewer CanContentScroll="True" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
              </ScrollViewer>
            </Border>
          </Popup>
        </Grid>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="DropDownToggle" Property="BorderBrush" Value="{DynamicResource Accent}"/></Trigger>
          <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="DropDownToggle" Property="BorderBrush" Value="{DynamicResource Accent}"/></Trigger>
          <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <ControlTemplate x:Key="GrepVerticalScrollBarTemplate" TargetType="{x:Type ScrollBar}"><Border Background="{DynamicResource HeaderBackground}" CornerRadius="4" Margin="2,0"><Track x:Name="PART_Track" IsDirectionReversed="True"><Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Opacity="0" Focusable="False"/></Track.DecreaseRepeatButton><Track.Thumb><Thumb><Thumb.Template><ControlTemplate TargetType="{x:Type Thumb}"><Border Background="{DynamicResource Secondary}" CornerRadius="4" Margin="1"/></ControlTemplate></Thumb.Template></Thumb></Track.Thumb><Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Opacity="0" Focusable="False"/></Track.IncreaseRepeatButton></Track></Border></ControlTemplate>
    <ControlTemplate x:Key="GrepHorizontalScrollBarTemplate" TargetType="{x:Type ScrollBar}"><Border Background="{DynamicResource HeaderBackground}" CornerRadius="4" Margin="0,2"><Track x:Name="PART_Track"><Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Opacity="0" Focusable="False"/></Track.DecreaseRepeatButton><Track.Thumb><Thumb><Thumb.Template><ControlTemplate TargetType="{x:Type Thumb}"><Border Background="{DynamicResource Secondary}" CornerRadius="4" Margin="1"/></ControlTemplate></Thumb.Template></Thumb></Track.Thumb><Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Opacity="0" Focusable="False"/></Track.IncreaseRepeatButton></Track></Border></ControlTemplate>
    <Style TargetType="{x:Type ScrollBar}"><Setter Property="Width" Value="8"/><Setter Property="Height" Value="8"/><Setter Property="Template" Value="{StaticResource GrepVerticalScrollBarTemplate}"/><Style.Triggers><Trigger Property="Orientation" Value="Vertical"><Setter Property="Width" Value="8"/><Setter Property="Height" Value="Auto"/><Setter Property="Template" Value="{StaticResource GrepVerticalScrollBarTemplate}"/></Trigger><Trigger Property="Orientation" Value="Horizontal"><Setter Property="Height" Value="8"/><Setter Property="Width" Value="Auto"/><Setter Property="Template" Value="{StaticResource GrepHorizontalScrollBarTemplate}"/></Trigger></Style.Triggers></Style>
  </Window.Resources>
  <Grid Margin="16"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid x:Name="TitleBar">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{x:Static res:Strings.Grep_Title}" FontSize="27" FontWeight="SemiBold" Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
        <Button x:Name="CloseButton" Grid.Column="2" Style="{StaticResource IconButton}" Content="&#xE8BB;" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,0,0,0" Click="Close_Click" Background="{DynamicResource Secondary}" ToolTip="{x:Static res:Strings.Common_Close}"/>
      </Grid>
      <TextBlock Grid.Row="1" Text="{x:Static res:Strings.Grep_ScopeHint}" Foreground="{DynamicResource Muted}" Margin="1,3,0,12"/>
    </Grid>
    <Border Grid.Row="1" BorderBrush="{DynamicResource Line}" BorderThickness="1" Background="{DynamicResource CardBackground}" Padding="12"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><ListBox x:Name="ScopeList" Height="86" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource Ink}"/><StackPanel Grid.Column="1" Margin="12,0,0,0"><Button Style="{StaticResource IconTextButton}" Click="ReloadScopes_Click" ToolTip="{x:Static res:Strings.Grep_ReloadSelection}">
            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE72C;" FontSize="15" VerticalAlignment="Center"/><TextBlock Text="{x:Static res:Strings.Grep_Reload}" Margin="6,0,0,0" VerticalAlignment="Center"/></StackPanel>
          </Button><TextBlock x:Name="ScopeCountText" HorizontalAlignment="Center" Foreground="{DynamicResource Muted}" Margin="0,9,0,0"/></StackPanel></Grid></Border>
    <Grid Grid.Row="2" Margin="0,14,0,10"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="180"/><ColumnDefinition Width="2*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{x:Static res:Strings.Grep_SearchText}" Foreground="{DynamicResource Muted}" Margin="0,0,0,5"/><history:HistoryTextBox HistoryKey="Grep-Query" x:Name="QueryBox" Height="36" Padding="8,4" VerticalContentAlignment="Center"/></StackPanel><StackPanel Grid.Column="1" Margin="12,0,0,0"><TextBlock Text="{x:Static res:Strings.Grep_LanguageProfile}" Foreground="{DynamicResource Muted}" Margin="0,0,0,5"/><ComboBox x:Name="ProfileBox" Height="36" Padding="8,4" VerticalContentAlignment="Center" SelectionChanged="ProfileBox_SelectionChanged"/></StackPanel><StackPanel Grid.Column="2" Margin="12,0,0,0"><TextBlock Text="{x:Static res:Strings.Grep_IncludedExtensions}" Foreground="{DynamicResource Muted}" Margin="0,0,0,5"/><history:HistoryTextBox HistoryKey="Grep-Extensions" x:Name="ExtensionsText" Height="36" Padding="8,4" VerticalContentAlignment="Center" IsReadOnly="True"/></StackPanel><StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="12,0,0,0"><Button x:Name="SearchButton" Style="{StaticResource IconTextButton}" Height="36" Click="Search_Click" ToolTip="{x:Static res:Strings.Common_Search}">
            <StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE721;" FontSize="15" VerticalAlignment="Center"/><TextBlock Text="{x:Static res:Strings.Common_Search}" Margin="6,0,0,0" VerticalAlignment="Center"/></StackPanel>
          </Button>
          <Button x:Name="CancelButton" Style="{StaticResource IconButton}" Margin="8,0,0,0" Background="{DynamicResource Secondary}" IsEnabled="False" Click="Cancel_Click" ToolTip="{x:Static res:Strings.Common_Cancel}" Content="&#xE711;"/></StackPanel></Grid>
    <DockPanel Grid.Row="3" Margin="0,0,0,10"><CheckBox x:Name="RegexBox" Content="{x:Static res:Strings.Grep_Regex}"/><CheckBox x:Name="MatchCaseBox" Content="{x:Static res:Strings.Grep_MatchCase}" Margin="18,0,0,0"/><CheckBox x:Name="WholeWordBox" Content="{x:Static res:Strings.Grep_WholeWord}" Margin="18,0,0,0"/></DockPanel>
    <Border Grid.Row="4" BorderBrush="{DynamicResource Line}" BorderThickness="1"><DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" GridLinesVisibility="Horizontal" HeadersVisibility="Column" Background="{DynamicResource CardBackground}" Foreground="{DynamicResource Ink}" BorderThickness="0" MouseDoubleClick="ResultsGrid_MouseDoubleClick"><DataGrid.Resources><Style x:Key="NumericCellStyle" TargetType="TextBlock"><Setter Property="HorizontalAlignment" Value="Stretch"/><Setter Property="TextAlignment" Value="Right"/><Setter Property="Margin" Value="0,0,8,0"/></Style></DataGrid.Resources><DataGrid.GroupStyle><GroupStyle><GroupStyle.ContainerStyle><Style TargetType="{x:Type GroupItem}"><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type GroupItem}"><StackPanel><ContentPresenter/><ItemsPresenter/></StackPanel></ControlTemplate></Setter.Value></Setter></Style></GroupStyle.ContainerStyle><GroupStyle.HeaderTemplate><DataTemplate><Border Background="{DynamicResource AccentSoft}" Padding="9,6" Margin="0,5,0,1"><TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource Accent}" TextTrimming="CharacterEllipsis"/></Border></DataTemplate></GroupStyle.HeaderTemplate></GroupStyle></DataGrid.GroupStyle><DataGrid.ColumnHeaderStyle><Style TargetType="DataGridColumnHeader"><Setter Property="Background" Value="{DynamicResource HeaderBackground}"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="BorderBrush" Value="{DynamicResource Line}"/><Setter Property="Padding" Value="8,6"/><Setter Property="FontWeight" Value="SemiBold"/></Style></DataGrid.ColumnHeaderStyle><DataGrid.RowStyle><Style TargetType="DataGridRow"><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="Background" Value="{DynamicResource CardBackground}"/><Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource ThemeSelected}"/></Trigger></Style.Triggers></Style></DataGrid.RowStyle><DataGrid.Columns><DataGridTextColumn Header="{x:Static res:Strings.Grep_Project}" Binding="{Binding ScopeName}" Width="150"/><DataGridTextColumn Header="{x:Static res:Strings.Grep_FileName}" Binding="{Binding FileName}" Width="220"/><DataGridTextColumn Header="{x:Static res:Strings.Grep_Line}" Binding="{Binding LineNumber}" Width="65" ElementStyle="{StaticResource NumericCellStyle}"/><DataGridTextColumn Header="{x:Static res:Strings.Grep_Column}" Binding="{Binding ColumnNumber}" Width="70" ElementStyle="{StaticResource NumericCellStyle}"/><DataGridTextColumn Header="{x:Static res:Strings.Grep_Match}" Binding="{Binding LineText}" Width="*"/></DataGrid.Columns></DataGrid></Border>
    <StackPanel Grid.Row="5" Margin="0,10,0,0">
      <ProgressBar x:Name="SearchProgress" Height="6" Margin="0,0,0,6" IsIndeterminate="True" Visibility="Collapsed" Foreground="{DynamicResource Accent}" Background="{DynamicResource HeaderBackground}"/>
    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><Grid.ColumnDefinitions><ColumnDefinition Width="Auto" MinWidth="160"/><ColumnDefinition Width="1.2*"/><ColumnDefinition Width="1.2*"/><ColumnDefinition Width="1*" MaxWidth="320"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBlock x:Name="StatusText" Grid.RowSpan="2" HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource Muted}" Text="{x:Static res:Strings.Common_Ready}" Margin="0,0,12,0" TextWrapping="Wrap" MaxWidth="280"/><TextBlock Grid.Column="2" Text="{x:Static res:Strings.Grep_Editor}" Margin="0,0,0,3" Foreground="{DynamicResource Muted}" FontSize="10"/><TextBlock Grid.Column="3" Text="{x:Static res:Strings.Grep_EditorArguments}" Margin="8,0,0,3" Foreground="{DynamicResource Muted}" FontSize="10"/><history:HistoryTextBox HistoryKey="Grep-Editor" PreserveOrder="True" Grid.Row="1" Grid.Column="2" x:Name="EditorBox" HorizontalContentAlignment="Left" TextAlignment="Left" TextWrapping="NoWrap" ToolTip="{x:Static res:Strings.Grep_EditorExecutable}"/><history:HistoryTextBox HistoryKey="Grep-EditorArguments" PreserveOrder="True" Grid.Row="1" Grid.Column="3" x:Name="EditorArgumentsBox" Margin="8,0,0,0" HorizontalContentAlignment="Left" TextAlignment="Left" TextWrapping="NoWrap"/><Button Grid.Row="1" Grid.Column="4" Style="{StaticResource IconButton}" Margin="8,0,0,0" VerticalAlignment="Stretch" Height="36" Click="BrowseEditor_Click" ToolTip="{x:Static res:Strings.Grep_BrowseEditor}" Content="&#xE8B7;"/></Grid>
    </StackPanel>
  </Grid>
</Window>
